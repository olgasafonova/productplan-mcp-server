name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  # Lint + race tests on the newest supported Go. Kept as a single,
  # non-matrix job because "check" is a required status context on main;
  # a matrix would rename it to "check (<go>)" and the required context
  # would never report.
  check:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v7

      - name: Set up Go
        uses: actions/setup-go@v7
        with:
          go-version: '1.27.x'
          cache: false  # No external dependencies, cache not needed

      - name: Install golangci-lint
        uses: golangci/golangci-lint-action@v9
        with:
          version: v2.13.2
          install-only: true

      - name: Run make check
        run: make check

  # Race tests on every supported Go release. go.mod declares go 1.26.0 (the
  # oldest supported line) so the module stays buildable there; this matrix
  # proves it. Gated through all-checks, not by per-leg required contexts.
  test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        go: ['1.26.x', '1.27.x']
    steps:
      - name: Checkout code
        uses: actions/checkout@v7

      - name: Set up Go
        uses: actions/setup-go@v7
        with:
          go-version: ${{ matrix.go }}
          cache: false

      - name: Run tests
        run: go test -race ./...

  security:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v7

      - name: Set up Go
        uses: actions/setup-go@v7
        with:
          go-version: '1.27.x'
          cache: false

      - name: Run Gosec
        uses: securego/gosec@master
        with:
          args: -exclude-generated ./...

      - name: Run govulncheck
        run: |
          go install golang.org/x/vuln/cmd/govulncheck@latest
          govulncheck ./... || echo "::warning::govulncheck found vulnerabilities (see above). Stdlib issues resolve with Go patch updates."

  go-mod-tidy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v7

      - name: Set up Go
        uses: actions/setup-go@v7
        with:
          go-version-file: go.mod
          cache: false

      - name: Check go mod tidy
        run: |
          go mod tidy
          git diff --exit-code go.mod go.sum

  build:
    runs-on: ubuntu-latest
    needs: [check, test, security]
    strategy:
      matrix:
        include:
          - goos: darwin
            goarch: amd64
          - goos: darwin
            goarch: arm64
          - goos: linux
            goarch: amd64
          - goos: linux
            goarch: arm64
          - goos: windows
            goarch: amd64

    steps:
      - name: Checkout code
        uses: actions/checkout@v7

      - name: Set up Go
        uses: actions/setup-go@v7
        with:
          go-version: '1.27.x'
          cache: false  # No external dependencies, cache not needed

      - name: Build
        env:
          GOOS: ${{ matrix.goos }}
          GOARCH: ${{ matrix.goarch }}
          CGO_ENABLED: 0
        run: go build -ldflags="-s -w" -o productplan-mcp-server ./cmd/productplan

      - name: Verify binary exists
        run: ls -la productplan-mcp-server*

  # Aggregator so the matrix build legs can be REQUIRED through one stable
  # context: branch protection cannot name 'build (os, arch)' legs robustly,
  # and before this job none of them gated a merge (bead claude-code-config-t68u).
  # Pattern from mediawiki-mcp-server PR #117.
  all-checks:
    name: all-checks
    if: always()
    needs: [check, test, security, go-mod-tidy, build]
    runs-on: ubuntu-latest
    steps:
      - name: Assert every dependency succeeded
        env:
          NEEDS: ${{ toJSON(needs) }}
        run: |
          printf '%s\n' "$NEEDS"
          bad=$(printf '%s' "$NEEDS" | jq -r 'to_entries[] | select(.value.result != "success") | "\(.key): \(.value.result)"')
          if [ -n "$bad" ]; then
            echo "::error::Aggregate failed; these jobs did not succeed:"
            printf '%s\n' "$bad"
            exit 1
          fi
          echo "All dependencies succeeded."
